--player = nil

function makePlayer()
  -- local p = {}
  -- p.x = 400
  -- p.y = 200
  -- p.speed = 3
  -- --p.sprite = love.graphics.sprite
  -- p.spriteSheet = love.graphics.newImage('sprites/rogue.png')
  -- p.grid = anim8.newGrid(24, 21, p.spriteSheet:getWidth(), p.spriteSheet:getHeight())
  -- return p
end